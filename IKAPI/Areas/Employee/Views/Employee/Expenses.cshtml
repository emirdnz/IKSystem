<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Masraf Yönetimi</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }

        .container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            max-width: 900px;
            margin: auto;
        }

        h2 {
            color: #007bff;
            font-weight: bold;
        }

        .form-select,
        .form-control {
            border-radius: 8px;
        }

        .form-label {
            font-weight: bold;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }

            .btn-primary:hover {
                background-color: #0056b3;
                border-color: #004085;
            }

        .table thead th {
            background-color: #007bff;
            color: #ffffff;
            text-align: center;
        }

        .table tbody tr:hover {
            background-color: #f1f1f1;
        }

        .btn-block {
            display: block;
            width: 100%;
        }

        .table-responsive {
            margin-top: 20px;
        }

        .btn-success,
        .btn-secondary {
            border-radius: 8px;
            font-weight: bold;
        }

        .btn-success {
            background-color: #28a745;
            border-color: #28a745;
        }

            .btn-success:hover {
                background-color: #218838;
                border-color: #1e7e34;
            }

        .btn-secondary {
            background-color: #6c757d;
            border-color: #6c757d;
        }

            .btn-secondary:hover {
                background-color: #5a6268;
                border-color: #545b62;
            }
    </style>
</head>

<body>
    <div class="container mt-5">
        <h2 class="mb-4 text-center">Masraf Yönetimi</h2>

        <!-- Masraf Tablosu -->
        <div class="table-responsive">
            <table class="table table-bordered table-hover" id="expensesTable">
                <thead>
                    <tr>
                        <th>Masraf Türü</th>
                        <th>Tutar</th>
                        <th>Tarih</th>
                        <th>Durum</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- Örnek Veriler -->
                    <tr>
                        <td>Yol Masrafı</td>
                        <td>150 TL</td>
                        <td>2024-08-25</td>
                        <td>Onaylı</td>
                    </tr>
                    <tr>
                        <td>Ofis Malzemeleri</td>
                        <td>500 TL</td>
                        <td>2024-08-24</td>
                        <td>Beklemede</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <!-- Yeni Masraf Girişi -->
        <h4 class="mt-4">Yeni Masraf Girişi</h4>
        <form asp-action="SubmitExpense" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="ExpenseType" class="form-label">Masraf Türü</label>
                <input type="text" class="form-control" id="ExpenseType" name="ExpenseType" />
            </div>

            <div class="form-group mt-3">
                <label for="Amount" class="form-label">Tutar</label>
                <input type="number" class="form-control" id="Amount" name="Amount" />
            </div>

            <div class="form-group mt-3">
                <label for="Date" class="form-label">Tarih</label>
                <input type="date" class="form-control" id="Date" name="Date" />
            </div>

            <div class="form-group mt-3">
                <label for="ReceiptFile" class="form-label">Fiş veya Belge</label>
                <input type="file" class="form-control" id="ReceiptFile" name="ReceiptFile" />
            </div>

            <button type="submit" class="btn btn-primary mt-4 btn-block">Gönder</button>
        </form>

        <!-- Geri Dön Butonu -->
        <div class="mt-4">
            <a href="/Employee/Employee/Index" class="btn btn-secondary">Geri Dön</a>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>

</html>

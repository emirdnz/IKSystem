<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>İşlem Logları</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            flex-direction: column; /* Dikey hizalama için ekledik */
        }

        .log-container {
            width: 80%;
            max-width: 800px;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px; /* Buton ile araya boşluk koymak için */
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
        }

        .log-entry {
            display: flex;
            justify-content: space-between;
            padding: 10px;
            border-bottom: 1px solid #ddd;
        }

        .log-entry:last-child {
            border-bottom: none;
        }

        .role {
            font-weight: bold;
            color: #007bff;
        }

        .timestamp {
            color: #555;
        }

        .action {
            color: #333;
        }

        .btn-primary {
            padding: 10px 20px;
            font-size: 16px;
            color: #fff;
            background-color: #007bff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: background-color 0.3s, transform 0.3s;
        }

        .btn-primary:hover {
            background-color: #0056b3;
            transform: translateY(-2px); /* Hover yapıldığında butonun yukarı hareket etmesi için */
        }

        .btn-primary:active {
            transform: translateY(0); /* Tıklanırken butonun eski yerine dönmesi için */
        }

    </style>
</head>
<body>
    <div class="log-container">
        <h2>Son İşlemler Logları</h2>
        <div class="log-entry">
            <span class="role">Admin</span>
            <span class="action">Kullanıcı ekledi</span>
            <span class="timestamp">2024-09-01 14:32</span>
        </div>
        <div class="log-entry">
            <span class="role">HR</span>
            <span class="action">Rapor güncelledi</span>
            <span class="timestamp">2024-09-01 13:45</span>
        </div>
        <div class="log-entry">
            <span class="role">Employee</span>
            <span class="action">İzin talebi oluşturdu</span>
            <span class="timestamp">2024-09-01 12:15</span>
        </div>
        <div class="log-entry">
            <span class="role">Admin</span>
            <span class="action">Sistem ayarlarını değiştirdi</span>
            <span class="timestamp">2024-09-01 11:00</span>
        </div>
        <!-- Daha fazla log girişi buraya eklenebilir -->
    </div>

    <!-- Geri Dön butonu -->
    <button onclick="window.location.href='@Url.Action("Login", "Admin" ,new { area = "Admin" })'" class="btn btn-primary mt-3">Geri Dön</button>

</body>
</html>
